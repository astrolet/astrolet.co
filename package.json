{
  "devDependencies": {
    "beverage": "0.0.2",
    "browser-sync": "^2.7.6",
    "coffeelint": "^1.10.1",
    "gulp": "^3.9.0"
  },
  "scripts": {
    "style": "compass compile -c compass.rb",
    "build": "npm run style && harp compile src www",
    "deploy": "surge www"
  }
}
